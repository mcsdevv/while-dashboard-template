# =============================================================================
# REQUIRED: These must be set for the application to work
# =============================================================================

# Google OAuth (single client for both authentication and Calendar API)
# Create a Web application OAuth 2.0 Client ID in Google Cloud Console
# Add redirect URI: https://your-domain.com/api/auth/callback/google
GOOGLE_CLIENT_ID=xxxxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxx

# Authentication
NEXTAUTH_SECRET=your-random-secret-here-generate-with-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000

# Authorization - email allowlist
# Comma-separated list of exact emails or wildcard patterns (spaces are trimmed)
# Examples:
#   - Exact: user@gmail.com
#   - Wildcard: *@company.com (allows any email from company.com)
AUTHORIZED_EMAILS=your-email@gmail.com,*@yourcompany.com

# Notion Integration
# Create an internal integration at https://www.notion.so/my-integrations
NOTION_API_TOKEN=secret_xxxxxxxxxxxxx

# Redis - Required for sync state and settings storage
# Auto-configured via Vercel Marketplace (Upstash integration)
# For local dev, connect to Vercel project or use a test Redis instance
UPSTASH_REDIS_REST_URL=https://xxxxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=xxxxxxxxxx

# =============================================================================
# OPTIONAL: Settings encryption (recommended for production)
# =============================================================================

# Encryption key for storing credentials in Redis (32-byte, base64 encoded)
# Generate with: openssl rand -base64 32
SETTINGS_ENCRYPTION_KEY=

# =============================================================================
# OPTIONAL: Notion Database ID (can be configured via web UI instead)
# =============================================================================

NOTION_DATABASE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# OPTIONAL: Webhooks & Admin
# =============================================================================

WEBHOOK_URL=https://your-domain.vercel.app/api/webhooks/google-calendar
ADMIN_SECRET=your-admin-secret
CRON_SECRET=your-cron-secret

# =============================================================================
# OPTIONAL: Development settings
# =============================================================================

NODE_ENV=development
LOG_LEVEL=info
